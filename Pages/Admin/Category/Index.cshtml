@page
@model MetaX.Pages.Admin.Category.IndexModel
@{
    Layout = "~/Pages/Shared/_Layout2.cshtml";
}

<div class="container float-start">
    <div class="row">
        <div class="col-md-12">
            <h2 class="text-left">Kategori Yönetimi</h2>
        </div>
    </div>

    <div class="row mt-4 justify-content-end">
        <div class="col-2 text-right align-self-end">
            <a href="/Admin/Category/Create" class="btn btn-success">
                <i class="fas fa-plus"></i> Yeni Kategori Ekle
            </a>
        </div>
    </div>
    <div class="row mt-5">
        <div class="col-3">
            <input type="text" id="searchInput" class="form-control" placeholder="Kategori Adı">
        </div>
        <div class="col-2">
            <a href="#" class="btn btn-primary w-100">
                <i class="fas fa-search"></i> Ara
            </a>
        </div>
        <div class="col-auto">
            <a href="#" class="btn btn-outline-dark">
                <i class="fas fa-times"></i>
            </a>
        </div>
    </div>
    <div class="row mt-4">
        <div class="col-md-12">
            <table class="table">
                <thead class="thead-light">
                    <tr>
                        <th scope="col">Kategori ID</th>
                        <th scope="col">Kategori Adı</th>
                        <th scope="col">İşlem</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model.CategoryListing)
                    {
                        <tr>
                            <td>@category.CategoryID</td>
                            <td>@category.Name</td>
                            <td>
                                <a asp-page="./Update" asp-route-id="@category.CategoryID" class="btn btn-sm btn-primary">Edit</a>
                                <a asp-page="./Delete" asp-route-id="@category.CategoryID" class="btn btn-sm btn-danger">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
